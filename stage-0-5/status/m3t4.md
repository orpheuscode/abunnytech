# m3t4 — Orchestrator Status

## Current state

- [x] Central config loader (`packages/config/`)
- [x] Pipeline runner with stage 0–4 support
- [x] Typer CLI with `run-stage{0..4}`, `run-demo`, `run-feedback-loop`, `status`
- [x] One-command demo script (`scripts/runbooks/demo.py`)
- [x] Kill switch (`KILL_SWITCH` env) and dry-run enforcement
- [x] Structured logging via structlog
- [x] Handoff doc (`docs/handoffs/m3t4.md`)

## Pending / future

- [ ] Real adapter wiring (non-mock) when providers are configured
- [ ] Stage 5 (monetize) integration in the pipeline (behind feature flag)
- [ ] HTTP control-plane endpoints (FastAPI app mounting all stage routers)
- [ ] Persistent run-history table for the dashboard to query
- [ ] Webhook / event hooks for real-time dashboard updates
