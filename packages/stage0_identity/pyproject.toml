[project]
name = "pipeline-stage0-identity"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pipeline-core",
    "pipeline-contracts",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pipeline_stage0_identity"]
